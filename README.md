# Which Pill, HealthHacks 2019 Entry

This small repo is intended to be the front-end to a ML model that classifies a pill based on a photo provided by the user.

The back-end (ML model) was created using Google's Cloud ML Vision service.
# which-pill

